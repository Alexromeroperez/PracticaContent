<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arp.practicacontent">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name=".Proveedor"
            android:authorities="com.arp.practicacontent.Proveedor"
            android:exported="false" />
        <provider
            android:name=".ProveedorCancion"
            android:authorities="com.arp.practicacontent.ProveedorCancion"
            android:exported="false" />
        <provider
            android:name=".ProveedorDisco"
            android:authorities="com.arp.practicacontent.ProveedorDisco"
            android:exported="false" />
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
